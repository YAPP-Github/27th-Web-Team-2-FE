pre-commit:
  commands:
    typecheck:
      run: npm run typecheck
    lint-staged:
      run: npx --no -- lint-staged --config lint-staged.config.mjs

commit-msg:
  commands:
    commitlint:
      run: npx --no -- commitlint --edit {1}
